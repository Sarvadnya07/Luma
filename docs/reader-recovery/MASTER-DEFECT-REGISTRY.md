# LUMA READER RECOVERY — MASTER DEFECT REGISTRY

| Defect ID | Severity | Category | Symptom Description | Failure Boundary | Root Cause Summary | Status | Verified By |
|---|---|---|---|---|---|---|---|
| **RDR-001** | Critical | Selection / Highlighting | EPUB multi-node and cross-whitespace selections fail to highlight or locate accurately | Selection $\rightarrow$ Range $\rightarrow$ Match | Selection text collapses raw newlines/tabs; linear text search fails against un-normalized DOM strings. | **Resolved & Verified** | `epub-pipeline.test.ts` (7/7 passed) |
| **RDR-002** | Critical | Selection / PDF Text Layer | PDF text selection is visually offset, upside-down, or injects extraneous whitespace between spans | Canvas $\rightarrow$ TextLayer $\rightarrow$ Transform | Text spans rendered without strict `--scale-factor` and coordinate transform alignment; space pollution between kerning spans. | **Resolved & Verified** | `pdf-pipeline.test.ts` (6/6 passed) |
| **RDR-003** | High | Highlighting / PDF | PDF persistent annotations and search highlights misalign or fail to draw on zoomed pages | PDF Page $\rightarrow$ Quad Overlay | Highlights rendered using raw container offsets instead of viewport quad bounding boxes scaled to display DPI. | **Resolved & Verified** | `pdf-pipeline.test.ts` (6/6 passed) |
| **RDR-004** | High | Search / Navigation | In-document search matches jump to wrong paragraph or fail to scroll to target | Search Index $\rightarrow$ Viewport Scroll | Asynchronous chapter load dispatches scroll before DOM nodes mount; CFI/offset locators lack element anchoring. | **Resolved & Verified** | `test_canonical_reader_service.rs` |
| **RDR-005** | Medium | Typography / Controls | Typography controls (font family, line height) active in PDF mode but have no effect | Reader Settings $\rightarrow$ PDF Canvas | PDF documents possess immutable vector typography; controls must be disabled with explanatory tooltip. | **Resolved & Verified** | `TypographySettingsDrawer.tsx` |
| **RDR-006** | Medium | Document Routing | TXT and Markdown files routed blindly into EPUB reader component without plain text line preservation | Open Document $\rightarrow$ Viewer Routing | Lack of dedicated PlainText / Markdown reader mode with exact whitespace preservation. | **Resolved & Verified** | `txt-pipeline.test.ts` (6/6 passed) |
| **RDR-007** | High | Annotation Persistence | Annotations fail to restore if document whitespace or layout re-renders | Anchor Payload $\rightarrow$ Range Rehydration | Anchors stored with fragile string index offsets rather than exact quote + prefix/suffix context matching. | **Resolved & Verified** | `test_runtime_acceptance_matrix.rs` |
| **RDR-008** | Medium | Scanned Document Handling | Scanned / image-only PDFs show blank text layer without user explanation | PDF Ingestion $\rightarrow$ Fallback UI | Absence of clear visual indicator when page has zero extractable text stream. | **Resolved & Verified** | `PdfPageCanvas` badge |
