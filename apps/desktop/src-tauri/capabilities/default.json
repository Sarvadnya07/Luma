{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for Luma reader desktop shell",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
